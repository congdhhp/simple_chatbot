# Core dependencies for the CLI chatbot - Ubuntu 24.04 WSL2 + CUDA 12.0 + RTX 5060 Ti
# PyTorch with CUDA 12.0 support (install separately via pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121)
torch==2.8.0
transformers==4.45.0
accelerate==1.10.1
pyyaml==6.0.2
click==8.2.1
colorama==0.4.6
rich==14.1.0
huggingface-hub==0.34.4
peft==0.17.1  # For LoRA adapter support

# Performance optimizations for GPU
bitsandbytes==0.47.0  # GPU-accelerated quantization
xformers==0.0.32.post2  # Memory-efficient attention
optimum==1.27.0  # Hardware-optimized inference

# Additional dependencies for better WSL2 compatibility
psutil==7.0.0  # System monitoring
nvidia-ml-py==13.580.65  # NVIDIA GPU monitoring
